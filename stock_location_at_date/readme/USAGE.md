To use this report:

1. Go to **Inventory > Reporting > Location Inventory At Date**.
2. Select the **Stock As Of Date** and optional filters (Location, Category, Product). Sub-locations and sub-categories are automatically included via `child_of` resolution.
3. Click **View Inventory** to view pre-aggregated stock quantities and valuations.
